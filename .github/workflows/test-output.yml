# DEPRECATED — functionality absorbed into data-writer composite action
# Intentionally inert: only manual dispatch, does nothing.
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: "true"
