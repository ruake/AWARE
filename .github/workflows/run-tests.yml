# DEPRECATED — replaced by controller.yml + job-playwright.yml + job-pytest.yml
# Intentionally inert: only manual dispatch, does nothing.
on: workflow_dispatch
jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - run: "true"
